{% extends 'base.html.twig' %}

{% block title %}Department{% endblock %}

{% block body %}
    <h1>Department</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ department.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ department.title }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ department.description }}</td>
            </tr>
            <tr>
                <th>Team_lead</th>
                <td>{{ department.teamLead }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-warning" href="{{ path('department_index') }}">back to list</a>

    <a class="btn btn-success" href="{{ path('department_edit', {'id': department.id}) }}">edit</a>

    {{ include('department/_delete_form.html.twig') }}
{% endblock %}
